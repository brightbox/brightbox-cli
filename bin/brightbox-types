#! /usr/bin/ruby1.8

begin
  require "bbcloud"
rescue LoadError
  bbcloud_path = File.expand_path('../../lib', __FILE__)
  $:.unshift(bbcloud_path)
  require "bbcloud"
end

Brightbox::BBConfig.config do
  include GLI
  run ARGV
end
